# Revision 참고 기록 — 2026-07-04 발견 사항

투고본(2026-06-26-submit1)의 결과에 영향을 주는 **중대한 발견**을 정리한다.
revision 버전 작성 시 반드시 반영할 것. 상세 근거·데이터는 개발 저장소
(`Tomo_SFTF_dev`)의 `DIAGNOSIS_2026-07-04_sftf_vs_uniform.md`,
`Tomo_Shell2026-dev/BUGREPORT_2026-07-03_INT16_vss_overflow.md`, 및 07-03~04 커밋.

---

## 0. 한 줄 요약

TOMO_CPU(정밀 계산기 DLL)에 **16비트 정수 오버플로 버그**가 있었다. 이 버그가
대형 메쉬의 지지부피(v_ss)를 음수로 망가뜨려 그 메쉬들을 평가에서 배제시켰고,
그 결과 **SFTF의 겉보기 우위가 인위적으로 부풀려져 있었다.** 버그를 고치고 전면
재계산하니, SFTF의 핵심 주장 일부가 무너진다. → 논문을 **유기형 부품 한정**으로
정직하게 축소해야 한다.

---

## 1. 발견한 버그 (근본 원인)

- 위치: `Tomo_Shell2026.dll`의 CPU 경로 `STomoSh_INT3.cpp`. 슬롯(복셀 컬럼)별
  z-좌표 합산 누산기가 `SLOT_BUFFER_TYPE = short int`(**int16, ±32,767**)였다.
- 증상: 면이 많은(≈10^6) 대형 메쉬의 복잡한 컬럼에서 z-합이 int16을 넘어 래핑 →
  **음수 수백만 스케일의 v_ss**. 예: Group_E3(105만 면) 1°/60° best v_ss = −3,105,304.
- 파급: 일반 메쉬(Bunny 등)도 높이 256 컬럼의 z-합(32,640)이 int16 한계 직하라
  상시 미세 오염 → **저장된 모든 TOMO_CPU 그리드가 오염 상태**였다.
- **수정 완료**: DLL을 int32 누산기로 고침(07-03 빌드, 검증됨). E3 음수 7,348개 → 0개,
  Bunny 등 전 셀 ~15~20% 변화. (부수적으로 CUDA 경로의 레이스·성능 회귀도 별도 수정.)

## 2. 논문 파이프라인에 준 영향

투고본은 "nonpositive-best(비양수 최적) 레코드 제외" 규칙으로 이 음수들을 걸렀는데,
**그 규칙이 걸러온 실체가 바로 이 오버플로**였다. 버그 수정 후 그 대형 메쉬들이
valid 집합에 편입되면서 결과가 구조적으로 바뀐다.

### (a) 살아남는 주장 (유기형 5메쉬 = 그룹 C 한정)
- 특징-v_ss 상관: **B(바닥판)가 최고 단일 예측자 유지** (평균 0.58→0.59),
  통합 R̃=R+B 최강 (0.60→0.61). — 표 3·4 숫자만 갱신.
- top-20 예산곡선: 평균 1.13→1.14, 예산 3.57%→3.40% (거의 불변).
- LOMO CV: SFTF tuned(1.71)가 PCA(1.95)·대칭 텐서(2.46)보다 우수 — 단 마진 축소
  (투고본 2.37 vs 3.63/4.30 → 1.71 vs 1.95/2.46). Manikin은 PCA가 근소 우세.

### (b) **무너지는 주장 (핵심)**
1. **budget-matched 우위 반전.** 투고본: "SFTF가 uniform/random보다 창을 잘 배치".
   정정 후 동일 예산(top-20 ~4%)에서, 전체 21메쉬:
   - SFTF top-20: 평균 3.34, 최대 34.0, ≤2.0 15/21
   - **uniform-axis: 평균 1.20, 최대 2.10, ≤2.0 20/21 (SFTF를 압도)**
   - random: 평균 1.32. 그룹 D(SFTF 6.85 vs uniform 1.09)·E(2.90 vs 1.44)에서도 uniform 우세.
   → "SFTF warm-start가 단순 커버리지보다 낫다"는 주장 **철회 필요**.
2. **대형 기계부품에서 특징이 v_ss를 예측 못 함.** D·E 그룹에서 모든 SFTF 특징의
   상관이 ≈0 (B는 E10에서 −0.40). 유기형(Bunny B=0.42)에서만 작동. → 순위가 무의미해져
   top-20이 3° NMS 바닥으로 뭉치고, 최적점은 pool 순위 41~255위(E2는 pool에 아예 없음).
3. **Happy가 더 이상 극단 하드케이스 아님.** Happy tomo_best 2112→8512(음수 오염이었음).
   top-10 비율 8.52→1.84. 투고본 그림/표의 "17991 vs 2112, ratio 8.52" 전부 오류.

### (c) 흥미로운 부산물
- **대칭 텐서 Rayleigh n^T M n** (논문이 "불충분"하다며 비대칭 SFTF로 대체한 그 baseline)이
  기계부품에서 오히려 더 나은 예측자 (전체 평균 상관 0.51 vs 비대칭 ≈0). 단 E9/E7에서
  붕괴하고, 순위로 써도 uniform은 못 이김. → "비대칭이 대칭보다 낫다"는 방법론 전제도
  기계부품에서는 성립 안 함.

## 3. revision에서 해야 할 일 (권고 = 정직한 스코프 축소)

- SFTF를 **"유기형 자유곡면 부품용 저비용·해석가능 후보 순위기"**로 재정의.
- budget-matched 우위 주장 철회, 대신 "유기형에선 단순 baseline도 충분, 차별점은
  해석가능성/속도"로 서술. 대형 기계부품은 **한계 사례로 정직하게 보고**(uniform이 우세 +
  특징 전이 실패 + 대칭 텐서 관찰).
- 모든 TOMO 의존 수치 재계산(개발 저장소에 반영 완료). AVE 검출기도 정정 데이터로
  재튜닝함(임계값 0.03→0.008, escalation 30/100→50/200, big-jump 트리거 추가).
- 그룹 E 대형 메쉬는 valid 편입되지만 SFTF가 약하고 AVE가 예산을 많이 씀(≈14%).

## 4. 개발 저장소 반영 상태 (참고)
`Tomo_SFTF_dev/draft/SFTF_draft.tex`·`SFTF_supplementary.tex`에 위 (a)(b)(c) 전부 반영·
컴파일 완료. 타이밍 표(t_CUDA)만 CUDA 재스윕 완료 후 갱신 예정. 커밋 로그 07-03~04 참고.
쉬운 설명본: `draft/SFTF_한계.tex`.
